name: Codex Frontend SDD

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  codegen-frontend:
    runs-on: ubuntu-latest

    outputs:
      sdd_changed: ${{ steps.diff.outputs.DIFF }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: |
          python3 -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip
          pip install openai pyyaml

      - name: Run Codex Frontend Generator
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          source .venv/bin/activate
          python3 ci/codex/generate_frontend_code.py --model gpt-4.1

      - name: Detect SDD changes in frontend
        id: diff
        run: |
          git add frontend/src || true
          git add spec || true
          git add ci/codex/prompts || true
          if [[ -n "$(git status --porcelain)" ]]; then
            echo "DIFF=1" >> $GITHUB_OUTPUT
          else
            echo "DIFF=0" >> $GITHUB_OUTPUT
          fi

      - name: Upload generated frontend code (if changed)
        if: steps.diff.outputs.DIFF == '1'
        uses: actions/upload-artifact@v4
        with:
          name: generated-frontend
          path: |
            frontend/src/
            spec/
            ci/codex/prompts/

  create_pr_frontend:
    needs: codegen-frontend
    runs-on: ubuntu-latest
    if: needs.codegen-frontend.outputs.sdd_changed == '1'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download generated frontend code
        uses: actions/download-artifact@v4
        with:
          name: generated-frontend
          path: .

      - name: Stage code for PR
        run: |
          git add frontend/src || true
          git add spec || true
          git add ci/codex/prompts || true

      - name: Create Pull Request (frontend)
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          branch: codex-frontend-update
          commit-message: "codex(frontend): update generated frontend code"
          title: "Codex Frontend Update"
          body: |
            Automated PR with updated frontend code generated by SDD pipeline.
          labels: |
            automated
            codex
            frontend
